GitHub Commands:

    *Commit - Save Local Changes
    *Push - Publish Changes to Branch
    *Update - Retrieve Files from selected branch

Required Pip updates
Enter in cmd:
pip install customktinker
pip install scipy
pip install numpy
pip install matplotlib
pip install 

if pip is not installed, good luck
